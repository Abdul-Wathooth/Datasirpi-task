To add the app.py application instead of nginx default.
- change directory to /etc/nginx/sites-available/default.config
- open with sudo
- In location remove the default /var/www/nginx/html and set our correct location.
- then open ( http://localhost )
